'use client';import React, { useState } from 'react';export default function InquiryPage() {  const [status, setStatus] = useState("");  const handleSubmit = async (e) => {    e.preventDefault();    setStatus("SENDING");        const formData = new FormData(e.target);    const name = formData.get('name');    const email = formData.get('email');    const userWhatsapp = formData.get('whatsapp');    const message = formData.get('message');    const payload = {      sender_name: name,      email: email,      message: `[User WhatsApp: ${userWhatsapp}] - ${message}`    };    try {      // 1. 存入您的 Supabase 数据库      await fetch("https://lvhbmoklunrsfnnvonsh.supabase.co/rest/v1/inquiries", {        method: 'POST',        headers: {          'Content-Type': 'application/json',          'apikey': 'sb_publishable_k0LxhDn7fEe70dm5G4KpfQ_YQ4ezKgq',          'Authorization': 'Bearer sb_publishable_k0LxhDn7fEe70dm5G4KpfQ_YQ4ezKgq',          'Prefer': 'return=minimal'        },        body: JSON.stringify(payload)      });      // 2. 自动跳转到您的 WhatsApp (带上客户填写的姓名和信息)
